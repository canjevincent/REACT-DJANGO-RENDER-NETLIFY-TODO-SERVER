Step 1 python -m venv venv
  # Install "Virtual Environment" and name the folder sa "venv"
Step 2 Run virtual environment
  # To activate environment run "./venv/scripts/activate"
Step 3 Upgrade pip version
  # To upgrade pip version run "python -m pip install --upgrade pip" command.
Step 4 Install "Django Rest Framework"
  # To install "Django Rest Framework" run "pip install djangorestframework"
Step 4 Install "Django Cors Headers"
  # To install "Django Cors headers" run "pip install django-cors-headers"
Step 5 Install mysqlclient
  # To install "mysqlclient" run "pip install mysqlclient"


"Use as default credentials for createsuperuser"
Username - admin
Email Address - admin@devmail.com
Password - adminaccess